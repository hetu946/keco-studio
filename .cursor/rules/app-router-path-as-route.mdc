---
description: Next.js App Router 下以 src/app 内文件相对路径推导 URL；Link/redirect/面包屑等与目录结构一致
globs: src/app/**/*.{tsx,ts}
alwaysApply: false
---

# 路由与文件路径一致

本仓库使用 Next.js App Router。**对外可访问的 URL 路径**应由 `src/app/` 下对应路由段的**目录相对路径**推导，避免与真实目录结构脱节的“手写路径”。

## 推导方式

- 以 `src/app/` 为根：其后每一级**文件夹名**（不含 `page.tsx`、`layout.tsx`、`loading.tsx`、`error.tsx`、`route.ts` 等文件名）按顺序组成路径段，用 `/` 连接即为主路径。
- **路由组**目录名形如 `(dashboard)`、`(marketing)`：**不出现在 URL 中**，推导时跳过该段文件夹名。
- **动态段**：文件夹名为 `[id]`、`[slug]` 等时，URL 中对应位置为实际参数值，文档或示例里可写为 `/.../:id` 或说明占位符。
- **可选 / 捕获**：`[[...slug]]`、`[...slug]` 等按 Next.js 约定映射，勿自造另一套规则。

## 编写导航与跳转时

- `Link` 的 `href`、`redirect()`、`router.push`/`replace`、服务端 `headers()` 相关重写、面包屑、sitemap 等**应能与当前或目标 `page.tsx` 所在目录一一对应**；新增页面时优先**从目标文件的父级路径抄出** href，再按需加 query/hash。
- 不要在已有并列目录结构的情况下，再维护一套与目录不一致的“路由常量表”作为主要真相来源；若已有常量，**变更目录时同步改常量**，或改为由路径片段拼装并与目录注释对齐。
- 内部跳转优先使用**以 `/` 开头的应用内绝对路径**（与浏览器地址栏一致），与 `src/app` 推导结果相同；避免把 `src/app` 物理路径当作 URL 前缀写出（例如不要写成 `/src/app/...`）。

## 示例

- 模拟器 UI 源码在**独立仓库** `keco-simulation`：`src/app/simulation-system/battle/skills/page.tsx`  
  - URL：`/simulation-system/battle/skills`
- Keco 主仓内仅保留嵌入壳：`src/app/(dashboard)/simulation-system/[[...segments]]/page.tsx` → 同上 URL（iframe 加载独立应用）
- 文件：`src/app/(dashboard)/projects/[projectId]/page.tsx`  
  - URL：`/projects/:projectId`（运行时替换为实际 id）

## 反例

- 目录已迁至 `battle/skills`，但 `Link` 仍指向旧的 `/battle-skill` 等**无法在 `src/app` 中找到对应文件夹链**的路径。
- 文档或注释里描述路由时**使用与 `src/app` 不一致的拼写或层级**，导致排查困难。
