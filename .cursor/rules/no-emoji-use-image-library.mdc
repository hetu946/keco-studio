---
description: 禁止使用 emoji；需要图片时使用项目图片库（src/assets/images），勿用图像生成工具充当素材
alwaysApply: true
---

# 沟通与图片资源

## Emoji

- 在回复、代码注释、提交说明、文档与 UI 文案示例中**不要使用 emoji**（含 Unicode 表情与装饰性符号）。
- 需要强调或分步说明时，用文字、标题层级或编号列表表达。

## 图片

- **不要**使用图像生成类工具产出“产品/界面用图”来替代正式素材。
- 需要配图、图标或插图时，**必须从项目已有图片库选用**，本仓库默认路径为 `src/assets/images/`（与现有 `*.svg` 等资产一致）。
- 引用方式与现有代码保持一致（例如 `import` 资源或项目约定的 public 路径）；**不要编造**仓库中不存在的文件名或 URL。
- 若库内无合适资源：用文字描述需求，并说明需设计/产品补充资产后再接入；不要临时捏造路径或外链占位。
